type_mappings:
  time.Time: "string"
  gorm.DeletedAt: "string | null"
  gorm.Model: "GormModel"

packages:
  - path: "github.com/AZ-Tokyo/AZ-Tokyo/backend/internal/model"
    output_path: "../frontend/src/types/model.ts"
    fallback_type: "any"
    frontmatter: |
      export interface GormModel {
        ID: number;
        CreatedAt: string;
        UpdatedAt: string;
        DeletedAt: string | null;
      }

